{
  "name": "agent-motel",
  "version": "0.1.0",
  "description": "Agent Motel — rooms for coding agents; the `am` CLI manages and jumps between Claude Code/Codex agents running in tmux",
  "license": "MIT",
  "type": "module",
  "bin": {
    "am": "src/index.ts"
  },
  "scripts": {
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
